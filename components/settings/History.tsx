import { CategoryContent } from './category-content';
export default function HistorySettings({ settings }: { settings: any }) {
  return <CategoryContent category='History' settings={settings} />;
}
