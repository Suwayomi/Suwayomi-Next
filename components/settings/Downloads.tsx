import { CategoryContent } from './category-content';
export default function DownloadsSettings({ settings }: { settings: any }) {
  return <CategoryContent category='Downloads' settings={settings} />;
}
