import { CategoryContent } from './category-content';
export default function TrackingSettings({ settings }: { settings: any }) {
  return <CategoryContent category='Tracking' settings={settings} />;
}
